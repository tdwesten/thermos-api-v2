{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug on Homestead",
            "type": "php",
            "request": "launch",
            "log": true,
            "pathMappings": {
                "/home/vagrant/code": "${workspaceFolder}"
            },
            "port": 9003
        }
    ]
}
